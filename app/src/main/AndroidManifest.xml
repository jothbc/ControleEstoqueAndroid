<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jcr.br.controleestoque">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_test"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_test"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ConsultaPrecoActivity"
            android:label="@string/title_activity_consulta_preco"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".ListLancadosActivity"
            android:label="@string/title_activity_list_lancados"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BaixaActivity"
            android:label="@string/title_activity_baixa"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>